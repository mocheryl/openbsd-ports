#!/bin/ksh

daemon="${TRUEPREFIX}/bin/athens"
daemon_logger="daemon.info"
daemon_user="_athens"
daemon_flags="-config_file ${SYSCONFDIR}/athens/config.toml"

. /etc/rc.d/rc.subr

rc_bg="YES"
rc_reload="NO"

rc_cmd $1
